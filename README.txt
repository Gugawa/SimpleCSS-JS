(PL)
Zadanie polegało na stworzeniu różnej wielkości div`ów w których umieszczone były dowolne teksty i obrazy.

Z początku wykonując zadanie w technikum obrazy umieszczone były ,,na sztywno" lokalnie jednak w celu ulepszenia wprowadziłem obecnie drobną modyfikację gdzie korzystając z API (https://picsum.photos) generowałem 4 obrazki w jakości HD (1280x720) i umieszczałem w CSS jako tła divów o odpowiednich klasach: f, h, j, k. Po każdorazowym odświeżeniu strony generowane są nowe obrazki.

Old Techstack: HTML, CSS
New Stack: HTML, CSS, JavaScript

-----------------------------------------------------------------------------------------------------------------
(EN)
The task was to create divs of various sizes in which any texts and images were placed.

At first, when performing the task in the technical school, the images were "rigidly" placed locally, however, in order to improve them, I have now introduced a slight modification where using the API (https://picsum.
photos) I generated 4 images in HD quality (1280x720) and placed them in CSS as backgrounds for divs with appropriate classes: f, h, j, k. New images are generated each time the page is refreshed.

Old Techstack: HTML, CSS
New Stacks: HTML, CSS, JavaScript